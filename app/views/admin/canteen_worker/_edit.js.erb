//有区域添加成功就添加显示列表中
<% if @operate_type == OPERATE_TYPE[:add] %>//添加
<% if @canteen_worker %>
var tbody = $("tbody");
tbody.find('tr.success').removeClass('success');
tbody.prepend('<%= escape_javascript(render_template_tr_show('admin/canteen_worker/tr.html.erb', {canteen_worker: @canteen_worker, tr_style: 'success'})) %>')
<%
end
else
if flush_length == 0
%>
$("tbody").find('tr.success').removeClass('success');
$('#' + '<%= "#{@canteen_worker.id}"%>').parents('tr').replaceWith('<%= escape_javascript(render_template_tr_show('admin/canteen_worker/tr.html.erb', {canteen_worker: @canteen_worker, tr_style: 'success'})) %>');

<%
end
end
%>

